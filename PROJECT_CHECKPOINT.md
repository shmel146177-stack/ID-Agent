# ID-Agent — восстановленная контрольная точка

Дата проверки: 19.08.2026  
Ветка: `develop`  
Исходная локальная база: `f005034` — `Use participant data in hidden works act signatures`

## Восстановленная последовательность

- `1c92a5a` — `Sanitize uploaded Windows paths on all platforms`
- `5572a20` — `Read drawing registers from visible PDF content`
- `df6a5d7` — `Add non-blocking training project mode`
- `34f0c22` — `Add managed supporting document uploads`
- `b3ffcf1` — `Preserve routed supporting documents`
- `7d6065a` — `Report supporting document routing conflicts`
- `5e2b0d4` — `Limit supporting document upload size`

## Проверено

- Кодовый набор без пяти тестов, зависящих от отсутствующих внешних PDF: `187 passed`.
- Windows-пути очищаются в обоих upload API на любой платформе.
- Ведомость рабочих чертежей читается по видимому содержимому PDF.
- Учебный режим проекта не блокирует обработку из-за архивных пропусков.
- Управляемая загрузка разделов 04–06 повторно запускает анализ и проверяет именно новый файл.
- Существующие маршрутизированные документы не перезаписываются; различающееся содержимое фиксируется как конфликт.
- Конфликт маршрутизации не маскируется статусом `Подтверждён`.
- Загрузка выполняется потоково с настраиваемым пределом; по умолчанию `512 MiB`.
- Частичный файл удаляется при ошибке копирования или превышении лимита.

## Внешние проверки

В текущем bundle отсутствуют локальные PDF, необходимые следующим тестам:

- `tests/test_classifier.py`
- `tests/test_document_analyzer.py`
- `tests/test_ocr_service_real.py`
- `tests/test_page_analysis_service.py`
- `tests/test_scanner.py`

Интеграционный тест checkpoint `c735d06` пока не перенесён: он требует фактических файлов `projects/Реальный_объект/analysis/project_analysis.json`, `page_analysis.json` и `supporting_documents_registry.json`. Тестовые подмены не использовать.

## Ограничения учебного проекта

- `Реальный_объект` должен оставаться в режиме `project_mode: training`.
- Отсутствующий лист №8 остаётся предупреждением и не блокирует разработку.
- Реальные документы разделов 04–06 нельзя заменять тестовыми данными.
- GitHub не использовать до отдельного решения пользователя.

## Следующий шаг

Установить этот bundle в локальный `C:\Projects\ID-Agent`, вернуть локальные PDF и данные `Реальный_объект`, выполнить полный `pytest`, затем перенести интеграционный тест `c735d06` и проверить первый подтверждённый реальный документ для каждого из разделов 04–06.
