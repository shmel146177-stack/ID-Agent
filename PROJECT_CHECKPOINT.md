# ID-Agent - контрольная точка

Дата проверки: 24.08.2026
Ветка: `develop`
Исходная контрольная точка: `b088f67` - `Strengthen supporting upload size limit checks`

## Состояние репозитория

- Локальная ветка `develop` синхронизирована с `origin/develop`.
- Последний подтвержденный коммит: `b088f67`.
- GitHub remote:
  `https://github.com/shmel146177-stack/ID-Agent.git`
- Репозиторий GitHub используется как основной удаленный репозиторий проекта.

## Регрессионная проверка

Полный набор тестов на 24.08.2026:

`192 passed, 1 skipped`

Пропущенный тест:

`tests/test_supporting_document_upload.py`

Причина:

`Создание симлинков недоступно в этой среде`

Это ограничение текущей Windows-среды, а не ошибка ID-Agent.

Проверка:

`git diff --check`

ошибок не выявила.

## Резервные материалы

Старые recovery-материалы перенесены из корня проекта в:

`backup/checkpoint-2026-08-24/`

Сохранены:

- `ID-Agent-d547e33-next-upload-size-limit.patch`
- `ID-Agent-recovered-311f35a/ID-Agent-recovered-311f35a.bundle`

Папка `backup/` исключена из Git.

## Подтвержденная функциональность

- Сканирование PDF через PyMuPDF.
- OCR через Tesseract `rus+eng`.
- Классификация документов.
- Анализ страниц и комплектности.
- Анализ ведомости рабочих чертежей.
- Карточка проекта.
- Учебный режим проекта.
- АОСР и сохранение данных актов.
- Журнал скрытых работ.
- Генерация исполнительной документации DOCX.
- Реестр документов Excel.
- Формирование ZIP-пакета проекта.
- `package_manifest.json`.
- Реестр сопроводительных документов.
- Разделы 04-06 исполнительной документации.
- Управляемая загрузка сопроводительных документов.
- Повторный анализ проекта после загрузки.
- Проверка соответствия загруженного документа разделу.
- Обнаружение конфликтов маршрутизации.
- Защита существующих маршрутизированных файлов от перезаписи.
- Потоковая загрузка сопроводительных документов.
- Максимальный размер сопроводительного файла по умолчанию: 512 MiB.
- Частичный файл удаляется при ошибке загрузки или превышении лимита.
- Превышение лимита через API возвращает HTTP 413.

## Учебный проект

`Реальный_объект` остается учебным проектом.

Отсутствующий лист №8 является предупреждением и не должен блокировать разработку.

Реальные документы проекта и результаты анализа хранятся локально в `projects/` и не отправляются в GitHub.

## Текущие технические задачи

До добавления новой функциональности необходимо устранить существующие несостыковки:

1. Привести общий `/projects/{project_name}/upload` к тем же правилам безопасности, что и управляемую загрузку сопроводительных документов:
   - ограничение размера;
   - потоковая запись;
   - запрет случайной перезаписи;
   - удаление частичного файла при ошибке;
   - корректные HTTP-коды.
2. Добавить автоматический запуск pytest через GitHub Actions.
3. Проверить необходимость старого интеграционного checkpoint `c735d06`.
4. Проверить реальный цикл документов разделов 04-06 на `Реальный_объект`.
5. После устранения технических несостыковок продолжить развитие ИИ-логики ID-Agent.

## Правило продолжения

До завершения перечисленных технических исправлений новую функциональность ID-Agent не добавлять.

После каждого изменения:

1. Запустить целевые тесты.
2. Запустить полный `python -m pytest -q`.
3. Выполнить `git diff --check`.
4. Проверить `git status --short`.
5. Только после успешной проверки создавать коммит.
